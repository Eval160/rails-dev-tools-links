<div class="row mt-5">
  <% @favorites.each do |favorite| %>
    <div class="col-12 col-lg-6 col-xl-4 p-4" id="resource-<%= favorite.resource.id %>">
      <%= render 'resources/card-index', resource: favorite.resource %>
    </div>
  <% end %>
</div>
